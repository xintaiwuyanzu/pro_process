logging:
  level:
    org:
      camunda: trace
server:
  port: 80
spring:
  datasource:
    autoDDl: update
    includeModules: sys,common,process
    password: 123123123
    url: jdbc:dm://127.0.0.1:5237?zeroDateTimeBehavior=convertToNull&useUnicode=true&characterEncoding=utf-8&schema=testProcess
    username: SYSDBA
    driver-class-name: dm.jdbc.driver.DmDriver
  servlet:
    multipart:
      max-file-size: 50MB
common:
  auto-init-data: true