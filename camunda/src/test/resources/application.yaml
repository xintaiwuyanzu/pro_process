logging:
  level:
    org:
      camunda: trace
server:
  port: 80
spring:
  datasource:
    autoDDl: update
    includeModules: sys,common,process
    password: 1234
    url: jdbc:mysql://127.0.0.1:3306/testProcess1?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
  servlet:
    multipart:
      max-file-size: 50MB
common:
  auto-init-data: true